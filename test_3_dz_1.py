"""
Задание 1
Погружение в Python | Коллекции
✔ Создайте вручную список с повторяющимися целыми числами.
✔ Сформируйте список с порядковыми номерами
нечётных элементов исходного списка.
✔ Нумерация начинается с единицы.
"""

def get_odd_positions():
    """
    Создаёт список с повторяющимися целыми числами,
    затем формирует и возвращает список порядковых номеров
    нечётных элементов (нумерация с единицы).
    """
    # 1. Задаём вручную список с повторяющимися целыми
    my_list = [4, 7, 2, 7, 5, 8, 5, 3, 2, 3, 7]

    # 2. Находим порядковые номера (i+1) для всех нечётных элементов
    odd_positions = [i + 1 for i, x in enumerate(my_list) if x % 2 != 0]

    return my_list, odd_positions

if __name__ == "__main__":
    original, positions = get_odd_positions()
    print("Исходный список:", original)
    print("Позиции нечётных элементов:", positions)